// import css from "./heading-flair?css-component";

export default {
  // css: css,

  name: "Heading Flair",

  // page: async () => (await import("./heading-flair.svelte")).default,

  scopes: {
    compatible: [
      "project",
      "profile",
      "jam",
    ],
  },

  wip: true,
} as ComponentData;
